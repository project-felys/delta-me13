services:
  vllm:
    image: vllm/vllm-openai:latest
    command: [
      "--model", "/models/PhiLia093-4B-FP8-BLOCK",
      "--served-model-name", "PhiLia093",
      "--max-model-len", "4096",
      "--max-num-seqs", "32",
      "--trust-remote-code",
      "--language-model-only",
      "--default-chat-template-kwargs", "{\"enable_thinking\": false}",
      "--gpu-memory-utilization", "0.70",
    ]
    ports:
      - "8000:8000"
    volumes:
      - "E:/Model:/models"
    ipc: host
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]